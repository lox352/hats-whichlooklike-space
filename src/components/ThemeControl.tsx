import { useEffect,useState } from "react";
export default function ThemeControl(){const [theme,setTheme]=useState(()=>{try{return localStorage.getItem('sky-theme')??'auto';}catch{return 'auto';}});useEffect(()=>{document.documentElement.dataset.theme=theme;try{localStorage.setItem('sky-theme',theme);}catch{/* preference only */}},[theme]);return <div className="theme-control screen-only"><label>Atlas appearance <select value={theme} onChange={e=>setTheme(e.target.value)}><option value="auto">Follow device</option><option value="night">Night</option><option value="paper">Parchment</option></select></label></div>;}
