import { expect, it } from "vitest";

// Placeholder until the first real suite lands, so `npm test` has something
// to run and the deploy gate is exercised from the first commit.
it("runs", () => {
  expect(1 + 1).toBe(2);
});
